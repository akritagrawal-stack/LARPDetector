{
  "name": "larp-detector-overlay",
  "version": "0.1.0",
  "description": "Floating macOS overlay for the LARP Detector: streams evidence, then renders an overall LARP score with founder and company breakdowns.",
  "private": true,
  "main": "electron/main.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "node --test electron/*.test.js",
    "start": "npm run build && electron .",
    "electron": "electron .",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "electron": "^43.2.0",
    "vite": "^6.0.5"
  }
}
